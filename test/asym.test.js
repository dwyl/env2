/**
 * This test is *designed* to test the scenario where a person does NOT
 * specify where their config.env file is located and they are calling
 * env2 as a module they installed fron npm and required from node_modules
 *
 */
 // setTimeout(function(){
  //  require('./node_modules/parent.test.js'); // executes file inside "fake" node_modules
 // },2000)
